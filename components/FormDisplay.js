$( document ).ready(function() {  // Vérifie que le document est loadé


    getResidentialInputFields()
})

// Get input field - OKAY
function getResidentialInputFields() {
    let residenialFields = $("#number-of-apartments")


    return residenialFields;
}